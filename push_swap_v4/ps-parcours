
demander a Pablo si on itere aussi sur la pile A
on entre dans la fonction ft_main_exec()
on recupere:
    la valeur du plus petit cout
    la combinaison gagnante de ce plus petit cout
    les deux index principaux de ce cout (pile A et pile B)

Reponse de Pablo: oui il y a une iteration sur la pile A et sur la pile B,
l'algorithme calcul le meilleur cout par rapport au contenu des deux piles A et B
ca prends du temps mais c'est optimise



synopsie du code:

on met toutes les valeurs du parsing dans la pile B sauf 3 valeurs
on trie ces 3 valeurs
on ajoute une par une les valeurs de la pile B en cherchant le cout le plus optimise

